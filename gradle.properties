# The project targets a Java 25 toolchain (for the java.lang.classfile ClassFile API, used to
# resolve "zoom lens" property selectors in the Topsoil ORM). The Gradle daemon itself still
# runs on the launcher JVM (Java 21); only compilation/tests/exec are forked onto JDK 25.
# This points Gradle's toolchain discovery at the locally installed Temurin 25.
org.gradle.java.installations.paths=/home/dan/.jdks/jdk-25.0.3+9
