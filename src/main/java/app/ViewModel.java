package app;

/**
 *  An identifier interface for all view models
 *  which is mostly important for the web framework
 *  so that it can distinguish between view models
 *  and data models (like color, string, etc.).
 */
public interface ViewModel
{

}
